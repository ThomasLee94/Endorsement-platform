
<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <a class="navbar-brand" href="/"> EndorseMe</a>
    {{#if currentUser}}
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav">
                <li class="nav-item">
                    <a class="nav-link" href="/activities">All Skills</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link disabled" href="#">{{currentUser.name}}'s' Profile</a>
                </li>
            </ul>
        </div>
    {{/if}}
    
    
    <ul class="nav navbar-nav navbar-right">
        {{#if currentUser}}
            <li><a href="/logout">Logout</a></li>
        {{else}}
             <li><a href="/sign-up">Sign-up</a></li>
        {{/if}}
    </ul>
</nav>

